/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['picsum.photos', 'api.escuelajs.co', 'i.imgur.com'], // thêm các domain bạn dùng
  },
};

module.exports = nextConfig;
